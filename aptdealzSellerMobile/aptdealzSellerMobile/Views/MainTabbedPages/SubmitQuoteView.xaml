<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="aptdealzSellerMobile.Views.MainTabbedPages.SubmitQuoteView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ext="clr-namespace:aptdealzSellerMobile.Extention">
    <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--#region Header(NavigationBar)-->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <Image
                    Grid.Column="0"
                    Margin="20,0,0,0"
                    HeightRequest="30"
                    HorizontalOptions="StartAndExpand"
                    Source="IconMenu.png"
                    VerticalOptions="CenterAndExpand">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="ImgMenu_Tapped" />
                    </Image.GestureRecognizers>
                </Image>

                <Image
                    Grid.Column="1"
                    Margin="0"
                    Aspect="AspectFill"
                    HeightRequest="45"
                    HorizontalOptions="CenterAndExpand"
                    Source="iconHeaderLogo.png"
                    VerticalOptions="CenterAndExpand"
                    WidthRequest="45" />

                <StackLayout
                    Grid.Column="2"
                    Margin="0,0,20,0"
                    HorizontalOptions="EndAndExpand"
                    Orientation="Horizontal">
                    <Image
                        Margin="0"
                        HeightRequest="30"
                        HorizontalOptions="CenterAndExpand"
                        Source="iconNotification.png"
                        VerticalOptions="CenterAndExpand">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="ImgNotification_Tapped" />
                        </Image.GestureRecognizers>
                    </Image>

                    <Image
                        Margin="0"
                        HeightRequest="30"
                        HorizontalOptions="CenterAndExpand"
                        Source="iconQuestion.png"
                        VerticalOptions="CenterAndExpand">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="ImgQuestion_Tapped" />
                        </Image.GestureRecognizers>
                    </Image>
                </StackLayout>
            </Grid>

            <BoxView
                Grid.Row="1"
                BackgroundColor="{StaticResource LightGray}"
                HeightRequest="1"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="EndAndExpand" />
            <!--#endregion-->

            <Grid Grid.Row="2" Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  BackArrow  -->
                <StackLayout
                    Grid.Row="0"
                    HorizontalOptions="FillAndExpand"
                    Orientation="Horizontal">
                    <Image
                        Margin="-5,0,0,0"
                        Aspect="AspectFit"
                        HeightRequest="25"
                        HorizontalOptions="CenterAndExpand"
                        Source="iconBackArrow.png"
                        VerticalOptions="CenterAndExpand"
                        WidthRequest="25">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="ImgBack_Tapped" />
                        </Image.GestureRecognizers>
                    </Image>

                    <!--  Quote Details  -->
                    <Label
                        Grid.Row="0"
                        Margin="-25,0,0,0"
                        Padding="0"
                        FontFamily="Poppins SemiBold"
                        FontSize="18"
                        HorizontalOptions="StartAndExpand"
                        LineBreakMode="WordWrap"
                        Text="Quote Details"
                        TextColor="{StaticResource Black}"
                        VerticalOptions="CenterAndExpand" />

                    <Label
                        Margin="0,0,20,0"
                        Padding="0"
                        FontFamily="Poppins SemiBold"
                        FontSize="18"
                        HorizontalOptions="EndAndExpand"
                        Text="REQ#123"
                        TextColor="{StaticResource Black}"
                        VerticalOptions="CenterAndExpand" />
                </StackLayout>

                <ScrollView
                    Grid.Row="1"
                    Margin="0"
                    HorizontalScrollBarVisibility="Never"
                    VerticalScrollBarVisibility="Never">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="20" />
                        </Grid.RowDefinitions>

                        <!--  Unit Price  -->
                        <StackLayout
                            Grid.Row="0"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <ext:ExtEntry
                                    x:Name="lblUnitQuotePrice"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="Unit Price"
                                    PlaceholderColor="{StaticResource Gray}"
                                    Style="{StaticResource entrPrimary}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  Handling Charge  -->
                        <StackLayout
                            Grid.Row="1"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <ext:ExtEntry
                                    x:Name="lblQuoteHandlingCharge"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="Handling Charge"
                                    PlaceholderColor="{StaticResource Gray}"
                                    Style="{StaticResource entrPrimary}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  Shiping Charge  -->
                        <StackLayout
                            Grid.Row="2"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <ext:ExtEntry
                                    x:Name="lblQuoteShippingCharge"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="Shiping Charge"
                                    PlaceholderColor="{StaticResource Gray}"
                                    Style="{StaticResource entrPrimary}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  QuoteValidity Date  -->
                        <StackLayout
                            Grid.Row="3"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <ext:ExtEntry
                                    x:Name="lblQuoteValidityDate"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="QuoteValidity Date"
                                    PlaceholderColor="{StaticResource Gray}"
                                    Style="{StaticResource entrPrimary}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  Shiping PIN Code  -->
                        <StackLayout
                            Grid.Row="4"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <ext:ExtEntry
                                    x:Name="lblQuoteProduct"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="Shiping PIN Code"
                                    PlaceholderColor="{StaticResource Gray}"
                                    Style="{StaticResource entrPrimary}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  Quote Validty Date  -->
                        <StackLayout
                            Grid.Row="5"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <ext:ExtEntry
                                    x:Name="lblValidityDate"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="Quote Validty Date"
                                    PlaceholderColor="{StaticResource Gray}"
                                    Style="{StaticResource entrPrimary}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  Country of origin  -->
                        <StackLayout
                            x:Name="frmQuoteProduct"
                            Grid.Row="6"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <Frame
                                Grid.Row="0"
                                Margin="30,10"
                                BackgroundColor="White"
                                Style="{StaticResource frmEntry}">
                                <Grid
                                    Margin="0"
                                    Padding="0"
                                    HorizontalOptions="FillAndExpand"
                                    VerticalOptions="FillAndExpand">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ext:ExtPicker
                                        x:Name="pckQuoteProduct"
                                        Title="Country of origin"
                                        Grid.Column="1"
                                        FontSize="{OnPlatform Android=14,
                                                              iOS=16}"
                                        TextColor="{StaticResource Black}">
                                        <Picker.Items>
                                            <x:String>Product1</x:String>
                                            <x:String>Product2</x:String>
                                            <x:String>Product3</x:String>
                                        </Picker.Items>

                                    </ext:ExtPicker>
                                    <ImageButton
                                        x:Name="ImgQuoteProductPck"
                                        Grid.Column="1"
                                        Margin="10,0,0,0"
                                        BackgroundColor="Transparent"
                                        Clicked="ImgProductPck_Clicked"
                                        HeightRequest="25"
                                        HorizontalOptions="EndAndExpand"
                                        Source="iconDownArrow.png"
                                        VerticalOptions="CenterAndExpand" />
                                </Grid>
                            </Frame>
                        </StackLayout>

                        <!--  Total Quote Amount  -->
                        <StackLayout
                            Grid.Row="7"
                            Margin="30,10"
                            HorizontalOptions="FillAndExpand">
                            <Grid Grid.Row="7" HorizontalOptions="FillAndExpand">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Label
                                    Grid.Column="0"
                                    FontFamily="Poppins SemiBold"
                                    FontSize="{OnPlatform Android=16,
                                                          iOS=18}"
                                    HorizontalOptions="FillAndExpand"
                                    Text="Total Quote Amount"
                                    TextColor="{StaticResource Black}"
                                    VerticalOptions="CenterAndExpand" />
                                <Label
                                    Grid.Column="1"
                                    FontFamily="Poppins SemiBold"
                                    FontSize="{OnPlatform Android=16,
                                                          iOS=18}"
                                    HorizontalOptions="EndAndExpand"
                                    Text="RS 3500"
                                    TextColor="{StaticResource Black}"
                                    VerticalOptions="CenterAndExpand" />
                            </Grid>
                        </StackLayout>

                        <!--  Comments  -->
                        <StackLayout
                            Grid.Row="8"
                            Margin="0"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="FillAndExpand">
                            <Frame
                                Margin="30,10"
                                Padding="20,10"
                                BackgroundColor="White"
                                HeightRequest="100"
                                Style="{StaticResource frmEntry}">
                                <Editor
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=14,
                                                          iOS=16}"
                                    Keyboard="Text"
                                    Placeholder="Comments:"
                                    PlaceholderColor="{StaticResource Gray}"
                                    TabIndex="1"
                                    TextColor="{StaticResource Black}" />
                            </Frame>
                        </StackLayout>

                        <!--  Submit Quote Button(42)  -->
                        <StackLayout
                            x:Name="btnSubmitQuoteTapped"
                            Grid.Row="9"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="CenterAndExpand">
                            <ext:CustomShadowFrame
                                x:Name="SubmitQuote"
                                Grid.Row="2"
                                Margin="20,0"
                                Padding="10,5"
                                BackgroundColor="{StaticResource Green}"
                                CornerRadius="5"
                                HorizontalOptions="FillAndExpand"
                                VerticalOptions="CenterAndExpand">
                                <Label
                                    Margin="15,10"
                                    FontFamily="Poppins"
                                    FontSize="{OnPlatform Android=16,
                                                          iOS=18}"
                                    HorizontalOptions="CenterAndExpand"
                                    Text="Submit Quote"
                                    TextColor="{StaticResource White}"
                                    VerticalOptions="CenterAndExpand" />
                                <ext:CustomShadowFrame.GestureRecognizers>
                                    <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="Submit_QuoteTapped" />
                                </ext:CustomShadowFrame.GestureRecognizers>
                            </ext:CustomShadowFrame>

                        </StackLayout>
                    </Grid>
                </ScrollView>
            </Grid>
        </Grid>
    </ContentView.Content>
</ContentView>